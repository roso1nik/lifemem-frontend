services:
  lifemem-frontend:
    image: ${FRONTEND_IMAGE}
    ports:
      - "127.0.0.1:${VPS_PORT}:8000"
    env_file:
      - .env
    restart: unless-stopped
    labels:
        io.portainer.accesscontrol.teams: "lifemem"
